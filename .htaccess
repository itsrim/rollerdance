# Fichier .htaccess désactivé - cause d'erreur 500 sur Free.fr
# Pour réactiver, décommenter les règles ci-dessous une par une en testant

# Redirection HTTPS - Décommenter quand le certificat SSL sera activé
# RewriteEngine On
# RewriteCond %{HTTPS} off [OR]
# RewriteCond %{HTTP_HOST} !^rollerdancestyle\.free\.fr$ [NC]
# RewriteRule ^(.*)$ https://rollerdancestyle.free.fr%{REQUEST_URI} [L,R=301]

# HSTS (HTTP Strict Transport Security) - Sécurité supplémentaire
# <IfModule mod_headers.c>
#     Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
# </IfModule>

# Protection des images via robots.txt uniquement (plus compatible Free.fr)

# Redirection HTTPS vers HTTP désactivée - cause d'erreur 500 sur Free.fr
# Les règles mod_rewrite ne sont pas supportées ou fonctionnent différemment
# Solution : gérer la redirection au niveau DNS/configuration serveur si nécessaire
